import React from 'react';
import { StyleSheet, Text, View, FlatList, Button } from 'react-native';

const TodosDetail = (props) => {

    const

    return (
        <View>
            <Button 

            />
        </View>
    );
}

export default TodosDetail;